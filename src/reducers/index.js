export const initialState = {
  item: "Learn about reducers",
  completed: false,
  id: 2342341324
};

export const taskReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
